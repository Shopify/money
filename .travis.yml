sudo: false
language: ruby
cache: bundler
branches:
  only:
  - master
rvm:
  - 2.7
  - 2.6
before_install:
  # https://github.com/travis-ci/travis-ci/issues/8978#issuecomment-354036443
  - gem update --system
  - gem install bundler
